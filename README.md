# Tapo C210 Client

Java で Wi-Fi カメラへ接続するクライアントです。開発ルールは [AGENTS.md](AGENTS.md) を参照してください。

## 前提環境

- Java 21 以上
- Maven 3.9 以上

## ビルドとテスト

```text
mvn verify
```

カメラ実機に依存しないテストを標準のビルドに含め、実機を使う統合テストは別途明示的に実行できる構成にします。

## 現在の状態

初期プロジェクト構成のみです。カメラの認証、接続方式、映像取得、PTZなどの仕様をテストとして合意してから実装を進めます。
