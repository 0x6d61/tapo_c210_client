# AGENTS.md

## プロジェクト概要

このリポジトリは、Java で Wi-Fi カメラ（当面は TP-Link Tapo C219 を想定）へ接続するクライアントを開発するためのものです。

- Java: 21 以上
- ビルド・依存関係管理: Maven
- 開発手法: TDD（テスト駆動開発）
- ブランチ戦略: GitHub Flow
- 既定ブランチ: `main`

カメラの接続方式、認証方式、対応する操作・ストリーム仕様など、未確定の要件は実装前にテスト可能な振る舞いとして整理します。仕様が曖昧なまま、実機依存のコードや大きな抽象化を先行して追加しないでください。

## 開発方針

### TDD

すべての振る舞いは、原則として次のサイクルで実装します。

1. **Red**: 期待する振る舞いを表す失敗テストを1つ追加する。
2. **Green**: テストを通す最小限の実装を行う。
3. **Refactor**: テストを通したまま設計・可読性・重複を改善する。

テストは実装の詳細ではなく、利用者から見た契約を検証します。各テストは単一の振る舞いに集中させ、テスト名から失敗理由が分かるようにします。

- 単体テストは `src/test/java` に置く。
- 実機なしで再現できるテストを標準の `mvn test` に含める。
- カメラ実機、ネットワーク、長時間ストリームなどを使うテストは、明示的な統合テストとして分離し、CIの通常実行では実機を要求しない。
- 認証情報、IPアドレス、録画データなどをテストコードやログにハードコードしない。
- 不安定な時刻、乱数、ネットワーク応答はテストダブルで制御する。

### 設計とセキュリティ

- ネットワーク境界とドメインロジックを分離し、ドメインロジックは実機なしでテストできるようにする。
- タイムアウト、リトライ、キャンセル、切断、部分的な応答を明示的に扱う。
- パスワードやトークンは標準出力・例外メッセージ・コミットに含めない。
- 外部ライブラリを追加する場合は、必要性、ライセンス、保守状況、脆弱性を確認し、`pom.xml` に最小限の範囲で追加する。
- 公開APIには、前提条件・失敗時の挙動・スレッド安全性をドキュメント化する。

## Mavenコマンド

プロジェクトルートで実行します。

```text
mvn test
mvn verify
mvn -DskipTests package
```

依存関係やプラグインを更新した場合は、少なくとも `mvn verify` を実行してください。テストを意図的に省略した場合は、理由をコミットまたはプルリクエストに記録します。

## リポジトリ構成

```text
src/main/java/   本番コード
src/test/java/   単体テスト
src/main/resources/ 設定・実行時リソース
src/test/resources/ テスト用リソース
pom.xml          Mavenプロジェクト定義
```

パッケージ名は `io.github.tapo.c219...` を基本とします。カメラ固有のプロトコル処理は、汎用的なドメインモデルやトランスポート層と混ぜず、責務を分けて配置します。

## GitHub Flow

- `main` は常にビルド可能・テスト可能な状態に保つ。
- 作業開始時に最新の `main` から短命なブランチを作る。
- ブランチ名は目的が分かるように `feature/<機能名>`、`fix/<不具合>`、`chore/<作業>` を使う。
- 変更は小さく保ち、TDDのまとまりごとにコミットする。
- `main` への変更はプルリクエスト経由とし、テスト結果と設計上の判断を説明する。
- CIが成功し、レビューで未解決の指摘がないことを確認してからマージする。
- マージ後は作業ブランチを削除する。

コミットメッセージは、変更内容が履歴だけで追える簡潔な命令形にします。不要なフォーマット変更や、依頼範囲外のファイルを同じコミットに含めません。

## 完了条件

変更を完了とするには、次を満たします。

- 期待する振る舞いを表すテストがある。
- `mvn verify` が成功する。
- 実機が必要な確認は、必要な環境と実施結果を記録している。
- セキュリティ上の秘密情報や不要な生成物をコミットしていない。
- 関連するREADME、JavaDoc、設定例が更新されている。
